import React from "react";
import { FilterBar } from "../components/FilterBar/FilterBar";

export default {
  title: "Filter Bar",
  component: FilterBar,
};

export const FilterBarComponent = () => <FilterBar></FilterBar>;
