import React from "react";
import { Button } from "../components/Package/subcomponents/Button";

export default {
  title: "Button",
  component: Button,
};

export const ViewPackageButton = () => (
  <Button text="VIEW PACKAGE" href="#"></Button>
);
